#' North West England districts
#'
#' `nwEngland_sp` is a `SpatialPolygons` object representing the 24 districts in North West England found in the `INLA::Leuk` dataset of INLA (also, the `spBayesSurv::LeukSurv`) from the data studied in Henderson et al. (2002). "Modeling spatial variation in leukaemia survival data". JASA, 97(460), 965-972. The object has been built using internal files from the `spBayesSurv` package.
#'
#' @details The `INLA::Leuk` dataset is used to illustrate the usage of the `scaleGMRF` package in the vignette `vignette("Leuk_case_study",package="scaleGMRF")`. `nwEngland_sp` is used to map the results for the districts.
#'
#'
#' @source  https://CRAN.R-project.org/package=spBayesSurv

"nwEngland_sp"
